
export const config = {
    API_URL: 'http://localhost:3001/api',
    USE_MOCK: true,
    // Add IDs of users who are developers/owners here.
    // 'user-1' is the default mock admin "Voyager".
    DEVELOPER_IDS: ['user-1']
};
